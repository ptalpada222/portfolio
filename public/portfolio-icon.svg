<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100">
  <!-- Background shape -->
  <rect width="100" height="100" rx="20" fill="#4a6cf7"/>
  
  <!-- Initials (change "AB" to your initials) -->
  <text x="50" y="65" font-family="Arial" font-size="50" font-weight="bold" fill="white" text-anchor="middle">PD</text>
  
  <!-- Optional: Decorative dot -->
  <circle cx="50" cy="25" r="10" fill="#ffffff" opacity="0.8"/>
</svg>